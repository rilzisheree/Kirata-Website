[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["BASE_PATH=/ pnpm --filter @workspace/bio run build"]

[start]
cmd = "BASE_PATH=/ pnpm --filter @workspace/bio run serve"
